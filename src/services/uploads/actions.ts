import * as actionTypes from './actionTypes'

export const uploadFile = (dispatch: Function) => Promise.resolve(actionTypes.UPLOAD_FILE)

export const getvideos = (dispatch: Function) => Promise.resolve('all guchi')